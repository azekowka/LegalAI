# Обновления в реальном времени и красные звездочки

Реализованы автоматические обновления документа при вводе данных в форму переменных и добавлены красные звездочки для обязательных полей.

## ✅ Что реализовано

### 1. **Красные звездочки для обязательных полей**
- ✅ В форме переменных на вкладке "Переменные"
- ✅ В боковой панели переменных
- ✅ В подсказках и описаниях
- ✅ В статистике

### 2. **Автоматическое обновление документа**
- ✅ При вводе в любое поле переменных документ обновляется мгновенно
- ✅ Синие подчеркнутые поля становятся черными при заполнении
- ✅ Незаполненные поля остаются синими и подчеркнутыми
- ✅ Обновление происходит без задержек

### 3. **Умное отображение переменных**
- ✅ Незаполненные: синий цвет (`#0066cc`) + подчеркивание
- ✅ Заполненные: черный цвет (`#000`) без подчеркивания
- ✅ Автоматическое переключение при вводе/удалении текста

## 🎯 Как это работает

### Логика обновления

```typescript
const handleVariableChange = (variableId: string, value: string) => {
  // 1. Обновляем данные
  const newTemplateData = {
    ...templateData,
    variables: { ...templateData.variables, [variableId]: value }
  };
  
  // 2. Автоматически обновляем документ
  const slateContent = ExactDocumentConverter.convertWithData(template, newTemplateData);
  setEditorContent(JSON.stringify(slateContent));
};
```

### Логика цветов

```typescript
// В ExactDocumentConverter
{
  text: variables.companyName || 'Название компании',
  color: variables.companyName ? '#000' : '#0066cc',  // Черный если заполнено, синий если нет
  underline: !variables.companyName                   // Подчеркивание только если не заполнено
}
```

## 🎨 Визуальные изменения

### Форма переменных
```
Название компании *     [поле ввода]
Город компании *        [поле ввода]  
Телефон компании *      [поле ввода]
...
Итоговая сумма         [поле ввода]  // без звездочки
```

### Документ в редакторе

**Незаполненное поле:**
```
Телефон: [Телефон компании]  // синий + подчеркнутый
```

**Заполненное поле:**
```
Телефон: +7 (777) 123-45-67  // черный, без подчеркивания
```

## 🔄 Процесс работы

### 1. Начальное состояние
- Все переменные в документе синие и подчеркнутые
- В форме показаны названия полей со звездочками для обязательных

### 2. При вводе данных
- Поле в форме заполняется
- Соответствующее поле в документе **мгновенно** меняется:
  - Цвет: синий → черный
  - Подчеркивание: есть → нет
  - Текст: название переменной → введенное значение

### 3. При очистке поля
- Поле в документе возвращается к исходному состоянию:
  - Цвет: черный → синий
  - Подчеркивание: нет → есть
  - Текст: введенное значение → название переменной

## 🎯 Пример использования

### Шаги тестирования:

1. **Откройте**: `http://localhost:3001/dashboard/templates/3`
2. **Перейдите**: На вкладку "Переменные"
3. **Увидите**: Все поля с красными звездочками для обязательных
4. **Введите**: Например, "ТОО Моя компания" в поле "Название компании *"
5. **Переключитесь**: На вкладку "Редактор"
6. **Увидите**: В документе "Название компании" стало черным и без подчеркивания
7. **Продолжите**: Заполнять другие поля и видеть мгновенные изменения

## 🔧 Технические детали

### Переменные в шаблоне
```typescript
// Все переменные из template1.ts:
- companyName *        // Название компании
- companyCity *        // Город компании  
- companyPhone *       // Телефон компании
- companyAddress *     // Адрес компании
- companyEmail *       // E-Mail компании
- companyBIN *         // БИН компании
- clientCompanyName *  // Название компании для КП
- authorPosition *     // Должность автора КП
- authorName *         // ФИО автора КП
- offerDate *          // Дата КП
- serviceDescription * // Описание услуг компании
- serviceType *        // Тип предлагаемых услуг
- totalAmount          // Итоговая сумма (необязательно)
```

### Автоматическое обновление
- Происходит при каждом изменении поля
- Использует `ExactDocumentConverter.convertWithData()`
- Обновляет `editorContent` состояние
- Rich-text-editor автоматически перерисовывается

## 🎉 Результат

Теперь у вас есть **полностью интерактивная система**:

1. ✅ **Красные звездочки** для обязательных полей
2. ✅ **Мгновенные обновления** документа при вводе
3. ✅ **Визуальная обратная связь** - синие поля становятся черными
4. ✅ **Точная копия** макета с картинки
5. ✅ **Удобный интерфейс** для заполнения переменных

### Пользовательский опыт:
- Вводите данные в форму → видите изменения в документе мгновенно
- Красные звездочки показывают какие поля обязательны
- Синие подчеркнутые поля показывают что еще нужно заполнить
- Заполненные поля выглядят как финальный документ

**Система готова к использованию!** 🚀
