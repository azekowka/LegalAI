# Интеграция шаблонов с Rich Text Editor в Dashboard

Полная интеграция системы шаблонов документов с rich-text-editor в dashboard для редактирования и создания документов.

## 🎯 Что реализовано

### 1. **Конвертер шаблонов в Slate формат**
- `apps/web/src/lib/template-to-slate-converter.ts`
- Преобразует структурированные шаблоны в формат Slate для редактирования
- Поддерживает все типы секций: заголовки, текст, таблицы, подписи, контакты
- Обрабатывает переменные и стили

### 2. **Страница редактирования шаблона**
- `apps/web/src/app/dashboard/templates/[id]/page.tsx`
- Полноценный редактор с тремя вкладками: Редактор, Переменные, Настройки
- Интеграция с rich-text-editor
- Боковая панель с переменными для быстрой вставки
- Предпросмотр документа с заполненными переменными

### 3. **Панель переменных шаблона**
- `apps/web/src/components/template-variables-panel.tsx`
- Поиск по переменным
- Группировка переменных (основные и дополнительные)
- Быстрая вставка переменных в редактор
- Копирование переменных в буфер обмена

### 4. **Обновленная страница шаблонов**
- `apps/web/src/app/dashboard/templates/page.tsx`
- Навигация к редактору шаблонов
- Клик по строке или кнопка "Редактировать" открывает редактор

## 🚀 Как использовать

### 1. Открытие шаблона для редактирования

```bash
# Переход в dashboard
http://localhost:3001/dashboard/templates

# Клик по шаблону "Коммерческое предложение"
# Или выбор "Редактировать" в dropdown меню
```

### 2. Работа в редакторе

#### Вкладка "Редактор"
- Полноценный rich-text-editor с форматированием
- Боковая панель с переменными справа
- Клик по переменной автоматически вставляет её в редактор

#### Вкладка "Переменные"  
- Просмотр всех доступных переменных
- Заполнение значений для предпросмотра
- Кнопка "Обновить редактор" применяет значения к содержимому

#### Вкладка "Настройки"
- Просмотр метаданных шаблона
- Статистика по переменным и секциям

### 3. Использование переменных

```typescript
// Формат переменных в редакторе
{{companyName}}        // Название компании
{{companyPhone}}       // Телефон компании  
{{clientCompanyName}}  // Название компании клиента
{{serviceDescription}} // Описание услуг
{{totalAmount}}        // Итоговая сумма
```

## 🔧 Технические детали

### Архитектура конвертера

```typescript
// Конвертация шаблона в Slate формат
const slateContent = TemplateToSlateConverter.convertTemplateToSlate(
  template,
  documentData
);

// Обратная конвертация для сохранения
const updatedTemplate = TemplateToSlateConverter.convertSlateToTemplate(
  slateContent,
  originalTemplate
);
```

### Структура Slate элементов

```typescript
// Заголовок
{
  type: 'heading-one',
  align: 'center',
  children: [{ text: 'Коммерческое предложение', bold: true }]
}

// Параграф с переменной
{
  type: 'paragraph',
  children: [{ text: 'Компания {{companyName}} предлагает услуги' }]
}

// Таблица (в текстовом формате)
{
  type: 'paragraph',
  children: [{ text: '# | Наименование | Стоимость', bold: true }]
}
```

### Обработка переменных

```typescript
// Вставка переменной в редактор
const handleVariableInsert = (variableId: string) => {
  const variableText = `{{${variableId}}}`;
  // Добавление в текущую позицию курсора
  insertTextAtCursor(variableText);
};

// Обработка переменных при конвертации
const processVariables = (content: string, variables: Record<string, any>) => {
  return content.replace(/{{(\w+)}}/g, (match, key) => {
    return variables[key] || '[Переменная не заполнена]';
  });
};
```

## 📋 Поддерживаемые возможности

### ✅ Реализовано
- [x] Конвертация шаблонов в Slate формат
- [x] Rich-text редактор с панелью инструментов
- [x] Боковая панель переменных
- [x] Поиск и фильтрация переменных
- [x] Вставка переменных в редактор
- [x] Предпросмотр с заполненными переменными
- [x] Сохранение изменений
- [x] Навигация между шаблонами

### 🔄 В разработке
- [ ] Продвинутая вставка переменных в позицию курсора
- [ ] Визуальное выделение переменных в редакторе
- [ ] Автодополнение переменных при вводе
- [ ] Валидация переменных в реальном времени
- [ ] Поддержка таблиц в Slate редакторе

### 🚀 Планируется
- [ ] Создание новых шаблонов через редактор
- [ ] Версионирование шаблонов
- [ ] Совместное редактирование
- [ ] Экспорт в различные форматы

## 🎨 UI/UX особенности

### Дизайн редактора
- Современный интерфейс в стиле Google Docs
- Адаптивная боковая панель переменных
- Удобная навигация по вкладкам
- Информативные уведомления

### Панель переменных
- Группировка по типам (основные/дополнительные)
- Поиск в реальном времени
- Показ типа и обязательности переменной
- Примеры использования

### Предпросмотр
- Открытие в новом окне
- Стили для печати
- Полное отображение документа

## 🔗 Связанные файлы

```
apps/web/src/
├── app/dashboard/templates/
│   ├── page.tsx                    # Список шаблонов
│   └── [id]/page.tsx              # Редактор шаблона
├── components/
│   ├── rich-text-editor.tsx       # Rich text редактор
│   └── template-variables-panel.tsx # Панель переменных
├── lib/
│   ├── template-processor.ts      # Обработка шаблонов
│   └── template-to-slate-converter.ts # Конвертер в Slate
├── templates/
│   └── template1.ts              # Шаблон коммерческого предложения
└── types/
    └── template.ts               # TypeScript типы
```

## 🐛 Известные ограничения

1. **Позиционирование курсора**: Вставка переменных происходит в конец документа, а не в позицию курсора
2. **Таблицы**: Отображаются в текстовом формате, нет визуального редактора таблиц
3. **Синхронизация**: Изменения в редакторе не сразу отражаются в структуре шаблона

## 🔧 Настройка и запуск

```bash
# Установка зависимостей
pnpm install

# Запуск в режиме разработки
pnpm dev

# Переход к редактору шаблонов
http://localhost:3001/dashboard/templates
```

## 📚 Примеры использования

### Создание нового шаблона
```typescript
const newTemplate: DocumentTemplate = {
  id: 'my-template',
  name: 'Мой шаблон',
  description: 'Описание шаблона',
  category: 'business',
  variables: [...],
  sections: [...],
  createdAt: new Date(),
  updatedAt: new Date()
};
```

### Работа с переменными
```typescript
// В редакторе
"Уважаемые представители {{clientCompanyName}}!"

// После обработки
"Уважаемые представители ТОО 'Клиент'!"
```

---

**Система готова к использованию!** 🎉

Теперь при открытии коммерческого предложения в `/dashboard/templates` вы увидите полноценный редактор с конвертированным шаблоном в rich-text-editor формате.
