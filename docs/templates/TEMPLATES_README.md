# Система шаблонов документов LegalAI

Полноценная система для создания документов на основе шаблонов с динамическими переменными, таблицами и генерацией PDF.

## 🚀 Возможности

- ✅ **Динамические шаблоны** - создание документов с переменными полями
- ✅ **Редактируемые таблицы** - добавление и редактирование табличных данных
- ✅ **Генерация PDF** - экспорт документов в PDF формат
- ✅ **Предпросмотр** - просмотр документа перед сохранением
- ✅ **Валидация** - проверка обязательных полей
- ✅ **Адаптивный дизайн** - работа на всех устройствах

## 📁 Структура файлов

```
apps/web/src/
├── types/
│   └── template.ts              # TypeScript типы для шаблонов
├── templates/
│   └── template1.ts             # Шаблон коммерческого предложения
├── lib/
│   └── template-processor.ts    # Процессор для обработки шаблонов
├── components/
│   ├── template-form.tsx        # Форма для заполнения шаблона
│   └── pdf-generator.tsx        # Компонент для генерации PDF
├── app/documents/create/
│   └── page.tsx                 # Главная страница создания документов
└── styles/
    └── document-templates.css   # Стили для документов
```

## 🎯 Использование

### 1. Создание нового шаблона

```typescript
// templates/my-template.ts
import { DocumentTemplate } from '../types/template';

export const myTemplate: DocumentTemplate = {
  id: 'my-template',
  name: 'Мой шаблон',
  description: 'Описание шаблона',
  category: 'business',
  variables: [
    {
      id: 'companyName',
      name: 'Название компании',
      type: 'text',
      required: true,
      placeholder: 'ООО "Моя компания"'
    }
  ],
  sections: [
    {
      id: 'header',
      type: 'header',
      content: 'Заголовок документа'
    },
    {
      id: 'content',
      type: 'text',
      content: 'Компания {{companyName}} предлагает...'
    }
  ],
  createdAt: new Date(),
  updatedAt: new Date()
};
```

### 2. Использование формы

```tsx
import TemplateForm from '../components/template-form';
import { myTemplate } from '../templates/my-template';

function MyPage() {
  const handleSave = (data) => {
    console.log('Сохраненные данные:', data);
  };

  const handlePreview = (html) => {
    console.log('HTML предпросмотр:', html);
  };

  return (
    <TemplateForm
      template={myTemplate}
      onSave={handleSave}
      onPreview={handlePreview}
    />
  );
}
```

### 3. Генерация PDF

```tsx
import PDFGenerator from '../components/pdf-generator';

function MyDocument() {
  const documentData = {
    templateId: 'my-template',
    variables: {
      companyName: 'ООО "Моя компания"'
    }
  };

  return (
    <PDFGenerator
      template={myTemplate}
      data={documentData}
    />
  );
}
```

## 📋 Типы переменных

### Текстовые поля
```typescript
{
  id: 'companyName',
  name: 'Название компании',
  type: 'text',
  required: true,
  placeholder: 'Введите название'
}
```

### Числовые поля
```typescript
{
  id: 'amount',
  name: 'Сумма',
  type: 'number',
  required: true,
  placeholder: '0'
}
```

### Даты
```typescript
{
  id: 'date',
  name: 'Дата',
  type: 'date',
  required: true,
  defaultValue: new Date().toISOString().split('T')[0]
}
```

### Выпадающие списки
```typescript
{
  id: 'category',
  name: 'Категория',
  type: 'select',
  required: true,
  options: ['Опция 1', 'Опция 2', 'Опция 3']
}
```

### Email и телефон
```typescript
{
  id: 'email',
  name: 'Email',
  type: 'email',
  required: true,
  placeholder: 'user@example.com'
},
{
  id: 'phone',
  name: 'Телефон',
  type: 'phone',
  required: true,
  placeholder: '+7 (777) 123-45-67'
}
```

## 📊 Работа с таблицами

### Определение таблицы в шаблоне
```typescript
{
  id: 'services-table',
  type: 'table',
  content: 'Список услуг',
  tableColumns: [
    {
      id: 'number',
      name: '#',
      type: 'number',
      editable: false
    },
    {
      id: 'service',
      name: 'Услуга',
      type: 'text',
      editable: true
    },
    {
      id: 'cost',
      name: 'Стоимость',
      type: 'currency',
      editable: true
    }
  ],
  tableRows: [
    {
      id: 'row-1',
      number: 1,
      service: 'Консультация',
      cost: 50000
    }
  ]
}
```

### Автоматический расчет итогов
```typescript
// Получение итоговых сумм
const totals = TemplateProcessor.calculateTableTotals(section, tableData);
console.log('Итого:', totals.cost);
```

## 🎨 Стилизация

### Применение стилей к секциям
```typescript
{
  id: 'header',
  type: 'header',
  content: 'Заголовок',
  style: {
    fontSize: '24px',
    fontWeight: 'bold',
    textAlign: 'center',
    margin: '20px 0'
  }
}
```

### Кастомные CSS классы
```css
.section-header {
  text-align: center;
  font-size: 18pt;
  font-weight: bold;
}

.document-table {
  width: 100%;
  border-collapse: collapse;
}
```

## 🔧 API TemplateProcessor

### Основные методы
```typescript
import TemplateProcessor from '../lib/template-processor';

// Обработка шаблона в текст
const text = TemplateProcessor.processTemplate(template, data);

// Обработка шаблона в HTML
const html = TemplateProcessor.processTemplateToHTML(template, data);

// Валидация данных
const errors = TemplateProcessor.validateTemplateData(template, data);

// Извлечение всех переменных
const variables = TemplateProcessor.extractAllVariables(template);

// Расчет итогов таблицы
const totals = TemplateProcessor.calculateTableTotals(section, tableData);
```

## 📱 Адаптивность

Система полностью адаптивна и работает на:
- 💻 Десктопных компьютерах
- 📱 Планшетах
- 📱 Мобильных устройствах

## 🖨️ Печать и PDF

### Браузерная печать в PDF
1. Нажмите кнопку "PDF"
2. Выберите "Сохранить как PDF"
3. Настройте параметры печати
4. Сохраните файл

### Настройки печати
- Формат: A4
- Ориентация: Книжная
- Поля: 15mm
- Масштаб: 100%

## 🚀 Запуск проекта

```bash
# Установка зависимостей
pnpm install

# Запуск в режиме разработки
pnpm dev

# Переход к странице создания документов
# http://localhost:3001/documents/create
```

## 🎯 Примеры шаблонов

### Коммерческое предложение (готово)
- ✅ Контактные данные компании
- ✅ Информация о клиенте
- ✅ Таблица услуг с ценами
- ✅ Автоматический расчет итогов
- ✅ Подпись и дата

### Договор (планируется)
- 🔄 Стороны договора
- 🔄 Предмет договора
- 🔄 Условия оплаты
- 🔄 Ответственность сторон

### Счет-фактура (планируется)
- 🔄 Реквизиты продавца и покупателя
- 🔄 Детализация товаров/услуг
- 🔄 Налоговые расчеты
- 🔄 Итоговые суммы

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License - используйте свободно в своих проектах!

---

**LegalAI** - Делаем создание документов простым и эффективным! 🚀
