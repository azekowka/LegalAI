# Руководство по тестированию интеграции шаблонов

## 🧪 Пошаговое тестирование

### 1. Запуск приложения

```bash
cd apps/web
pnpm dev
```

Приложение будет доступно по адресу: `http://localhost:3001`

### 2. Переход к шаблонам

1. Откройте `http://localhost:3001/dashboard/templates`
2. Вы должны увидеть список шаблонов
3. Найдите шаблон "Коммерческое предложение"

### 3. Открытие редактора

**Способ 1: Клик по строке**
- Нажмите на строку с шаблоном "Коммерческое предложение"

**Способ 2: Меню действий**
- Нажмите на иконку с тремя точками (⋯) в строке шаблона
- Выберите "Редактировать"

### 4. Проверка редактора

После открытия редактора вы должны увидеть:

#### Заголовок страницы
- ✅ Кнопка "Назад" 
- ✅ Название шаблона: "Коммерческое предложение"
- ✅ Описание шаблона
- ✅ Бейдж категории "business"
- ✅ Кнопки "Предпросмотр" и "Сохранить"

#### Вкладки
- ✅ "Редактор" (активная по умолчанию)
- ✅ "Переменные" 
- ✅ "Настройки"

#### Rich Text Editor
- ✅ Панель инструментов с кнопками форматирования
- ✅ Область редактирования в стиле документа
- ✅ Содержимое шаблона должно быть загружено и отображено

#### Боковая панель переменных
- ✅ Заголовок "Переменные шаблона"
- ✅ Поле поиска
- ✅ Информационное сообщение о том, как использовать переменные
- ✅ Секция "Основные переменные" с переменными:
  - companyName (Название компании)
  - companyCity (Город компании) 
  - companyPhone (Телефон компании)
  - companyAddress (Адрес компании)
  - companyEmail (E-Mail компании)
  - companyBIN (БИН компании)
  - clientCompanyName (Название компании для КП)
  - authorPosition (Должность автора КП)
  - authorName (ФИО автора КП)
  - offerDate (Дата КП)
- ✅ Секция "Дополнительные переменные" с переменными:
  - serviceDescription (Описание услуг компании)
  - serviceType (Тип предлагаемых услуг)
  - totalAmount (Итоговая сумма)

### 5. Тестирование функций

#### Вставка переменных
1. Нажмите на любую переменную в боковой панели
2. ✅ Должно появиться уведомление об успешной вставке
3. ✅ Переменная должна добавиться в редактор в формате `{{variableName}}`

#### Поиск переменных
1. Введите текст в поле поиска (например, "компания")
2. ✅ Список переменных должен отфильтроваться
3. ✅ Должны остаться только переменные, содержащие поисковый запрос

#### Копирование переменных
1. Наведите курсор на переменную
2. ✅ Должна появиться кнопка копирования
3. Нажмите на кнопку копирования
4. ✅ Должно появиться уведомление об успешном копировании

### 6. Тестирование вкладки "Переменные"

1. Перейдите на вкладку "Переменные"
2. ✅ Должны отобразиться поля для заполнения всех переменных
3. Заполните несколько полей (например, название компании)
4. Нажмите кнопку "Обновить редактор"
5. ✅ Вернитесь на вкладку "Редактор"
6. ✅ Переменные в тексте должны быть заменены на введенные значения

### 7. Тестирование предпросмотра

1. Нажмите кнопку "Предпросмотр"
2. ✅ Должно открыться новое окно
3. ✅ В окне должен отобразиться документ с заполненными переменными
4. ✅ Документ должен быть оформлен в стиле коммерческого предложения

### 8. Тестирование сохранения

1. Внесите изменения в редактор
2. Нажмите кнопку "Сохранить"
3. ✅ Должно появиться уведомление об успешном сохранении
4. ✅ Кнопка должна показать индикатор загрузки во время сохранения

### 9. Тестирование вкладки "Настройки"

1. Перейдите на вкладку "Настройки"
2. ✅ Должна отобразиться основная информация о шаблоне
3. ✅ Должна отобразиться статистика (количество переменных, секций, даты)

## 🐛 Что проверить на ошибки

### Консоль браузера
- Откройте Developer Tools (F12)
- Перейдите на вкладку Console
- ✅ Не должно быть красных ошибок
- ⚠️ Предупреждения допустимы

### Сеть
- Перейдите на вкладку Network в Developer Tools
- ✅ Все запросы должны выполняться успешно (статус 200)

### Производительность
- ✅ Переключение между вкладками должно быть быстрым
- ✅ Редактор должен отзывчиво реагировать на ввод
- ✅ Поиск переменных должен работать мгновенно

## 🎯 Ожидаемый результат

После успешного тестирования вы должны получить:

1. **Работающий редактор шаблонов** с конвертированным содержимым коммерческого предложения
2. **Интерактивную панель переменных** с возможностью поиска и вставки
3. **Систему предпросмотра** документов с заполненными переменными
4. **Удобную навигацию** между различными аспектами редактирования шаблона

## 🔧 Решение проблем

### Если шаблон не загружается
- Проверьте консоль на ошибки
- Убедитесь, что файл `template1.ts` существует и корректен

### Если переменные не вставляются
- Проверьте, что обработчик `handleVariableInsert` работает
- Проверьте формат содержимого редактора в консоли

### Если предпросмотр не работает
- Убедитесь, что блокировщик всплывающих окон отключен
- Проверьте консоль на ошибки в `TemplateProcessor`

### Если стили отображаются некорректно
- Убедитесь, что файл `document-templates.css` подключен
- Проверьте, что Tailwind CSS работает корректно

---

**Успешного тестирования!** 🚀

Если все тесты прошли успешно, система полностью готова к использованию.
